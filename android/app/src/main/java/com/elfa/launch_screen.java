package com.elfa;

import android.annotation.SuppressLint;
import android.os.Bundle;
import android.widget.TextView;

import androidx.annotation.Nullable;
import androidx.appcompat.app.AppCompatActivity;

import com.airbnb.lottie.LottieAnimationView;

import org.w3c.dom.Text;

public class launch_screen extends AppCompatActivity {

    // LottieAnimationView lottieAnimationView;
    // Typewriter textView;

    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
    // setContentView(R.layout.launch_screen);
//        Typewriter writer = new Typewriter(this);
//        setContentView(writer);

        // lottieAnimationView=findViewById(R.id.splash_icon_lottie);

        // lottieAnimationView.animate().translationY(1400).setDuration(1000).setStartDelay(4000);

        // textView = findViewById(R.id.app_name);
        // //Add a character every 150ms
        // textView.setText("");
        // textView.setCharacterDelay(15000);
        // textView.animateText("e-learning for all");
    }

}
